<?php
namespace Chamilo\Libraries\Architecture\Interfaces;

/**
 * A class implements the <code>NoContextComponent</code> interface to
 * indicate that it is a special component that runs into an application but can not show the applications
 * name in the breadcrumbs
 * 
 * @author Sven Vanpoucke
 */
interface NoContextComponent
{
}
