{# form.html.twig #}
{% form_theme form 'form.bootstrap.html.twig' %}

{{ form_start(form) }}
<div class="input-group">
    {% for field in form %}
        {{ form_widget(field) }}
    {% endfor %}
</div>
{{ form_end(form) }}